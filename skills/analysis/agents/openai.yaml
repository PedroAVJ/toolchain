interface:
  display_name: "Requirements Analysis"
  short_description: "Extract requirements, action items, and conflicts"
  default_prompt: "Use $analysis to produce Requirements and Action Items from this evidence and iterate with me."
